#include <stdio.h>
#include <stdlib.h>

#include "calc.h"
#include "stack.h"

int calc_parse(Stack s, const char* string) {

    return 0;
}

void calc_printMessageCode(int code) {
    switch (code) {
    }
}
